import { px, DEVICE_WIDTH } from '../vender'

const style = {
  btn: {
    x: px(10),
    y: (DEVICE_WIDTH - px(20)) / 2,
  },
}

const method = {}

const layout = {
  $refs: {},
  render(vm) {},
}

export { layout, style, method }
